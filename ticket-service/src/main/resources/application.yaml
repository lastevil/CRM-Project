server:
  port: 8704
  servlet:
    context-path: /ticket
spring:
  application:
    name: ticket-service
  datasource:
    url: jdbc:postgresql://localhost:5432/unicrmdb
    username: postgres
    password: Qwe123
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: tickets_schema
  liquibase:
    change-log: db/changelog/db.changelog-master.xml
    enabled: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    consumer:
      group-id: 1
    bootstrap-servers: localhost:9092
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8700/eureka}
      fetchRegistry: false